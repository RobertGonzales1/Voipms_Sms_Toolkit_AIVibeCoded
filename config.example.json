{
  "_comment": "Copy to config.json and fill in. PREFER environment variables for the two credential fields - this folder is inside OneDrive and config.json would sync to the cloud.",

  "api_username": "you@example.com",
  "api_password": "YOUR_VOIPMS_API_PASSWORD",

  "min_balance": 5.0,
  "stale_inbound_days": 0,
  "check_registration": true,
  "timeout_seconds": 30
}
